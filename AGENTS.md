1.使用真实远端生产数据，不要使用mock数据除非用户指定
2.构建部署到服务器时要注意包体名称的反斜杠与微信云托管要求一致
3.新增界面时字体要严格遵守docs文档下的前端字体规范，除非用户明确单独提出要求，否则字体规范优先级大于figma文件或其他参考。
4.注意微信原生 button 自带最小宽度、内边距、行高、边框伪元素、加载态和按压变换。这些默认样式在 Grid、绝对定位、旋转贴纸等复杂布局里不一定能被完全覆盖，导致按钮看起来被撑大、重叠或越界。业务功能按钮可以原生，强视觉和强布局约束的控件不要依赖微信原生按钮外观。
# ADR 0007: 同意后的最小化生产分析

状态：Accepted，2026-07-25。

产品分析默认关闭，只在用户明确同意后启用。客户端限制事件条数、字节、批大小和退避；撤回同意立即清空本地队列。前后端都拒绝身份、Token、照片/签名地址、昵称、自由文本、精确位置和业务对象 ID。

服务端仅保存以独立盐生成的 HMAC userHash，不保存原始 user ID，默认 90 天清理，注销时同步删除。关键业务转化以服务端的媒体、记录、邀请和会话事实对账，不把客户端点击当成成功。
